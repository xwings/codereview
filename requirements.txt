# The multi-agent harness. Everything else is stdlib, plus the `gh` and `git`
# CLIs. Not on PyPI, and the released wheel is not enough: the panel needs the
# orchestrator-rotation fixes that live on kerness's `dev` branch. Build from
# the working clone this repo keeps beside it (gitignored, its own repository):
#   git clone -b dev git@github.com:xwings/kerness.git vendor/kerness
#   cd vendor/kerness/bindings/python && ../../../../.venv/bin/maturin develop --release
kerness
